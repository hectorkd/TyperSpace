import React, { ReactNode } from 'react';
import './styles/Race.scss';

type Props = {
  socket: any, 
  setSocket: any, 
  children?: ReactNode
};

const Race: React.FC<Props> = (props) => {
  return <div></div>;
};

export default Race;
